name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master  # Change this if you want to trigger on a different branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Copy index.html to gh-pages base dir
        run: cp index.html /index.html  # Copy index.html to output directory

      - name: Copy problem mappings.js to gh-pages base dir
        run: cp problem_mapping.js /problem_mapping.js


      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.TOKEN }}
          publish_dir: ./index.html  # Change this to the actual directory where your built files are located

